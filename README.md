# SerialPlot
A simple python script to plot incomming data from a serial port

positional arguments:
  port
  baudrate

optional arguments:
  -h, --help
  --columns COLUMNS
  --history HISTORY
  --min MIN
  --max MAX
  --interval INTERVAL
  --separator SEPARATOR
